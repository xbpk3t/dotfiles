---
---

```yaml
# PLAN: [2026-03-04] 尝试用 dify + skills，做个知识库，来模仿 帽子姐和储殷 的话术（注意是话术）
# https://www.zhihu.com/question/1895000051002085554/answer/1972070527163172317
# ## 1) 按“一问一答”分组总结（你的问题 vs 我的回答）
#
#### ① 你：想做一个 skill 来模仿“托卡马克之冠（帽子姐）”文风，先要我给文风框架；并要求我先对齐理解、别问蠢问题、最好自己从多篇文章总结
#
#**我：**我把“模仿文风”拆成三层（表层语言 / 论证结构 / 叙述人格），说明要做的是“写作协议”而不是口头禅；并说明我会从多篇文本抽样归纳，交付物建议包含：风格规格书、生成器模板、审稿 rubric、few-shot 示例包。
#
#---
#
#### ② 你：把知乎链接的正文贴出来，并说明：前两个（体裁与立场强度）希望参数控制；第三个希望可控（你提供资料和核心论点）；问我能不能直接给 skill
#
#**我：**给了一个可直接落地的 skill 方案：HatLitPoliWriter（生成器）+ 可选 HatLitPoliCritic（审稿器），包含参数表、写作器提示词（结构节奏/段落推进/修辞规则/事实引用规则/自检重写机制）、用户调用模板，以及审稿器提示词。
#
#---
#
#### ③ 你：如果想最大程度模仿，是否要把全部文章做成知识库再结合 skill？模仿其他人是否也是同流程？还有没有优化项？
#
#**我：**回答“是，但关键不是拼原句，而是稳定复现机制”；建议做“双知识库”：风格库（取型不取句）+ 内容库（你给事实材料），再配 rubric 自动重写 + 防复刻闸门（重合度检测超阈值强制改写）。并指出模仿其他人基本同一条流水线：收集→清洗→标注写作动作→提炼 spec→实现 skill→审稿与防复刻。
#
#---
#
#### ④ 你：知识库方案该选哪个？是否有非 desktop（webapp/cli）的？知识库方案是否不如 desktop 好用？
#
#**我：**给出“非 desktop”路线与推荐：优先 Dify（WebApp + API，适合双库、metadata 过滤、服务化）；也给 CLI 路线（llamaindex-cli + Chroma）和偏证据链/复杂文档的 RAGFlow 等；说明 desktop 优势是零部署快试，Web/CLI 优势是可脚本化、可服务化、可流水线化，更适合你这种长期产出、参数控制的写作系统。
#
#---
#
#### ⑤ 你：为啥不用 n8n？n8n 不是知识库吗？把 n8n 和另一个（我当时主要对比的是 Dify）从知识库角度比较
#
#**我：**解释定位差异：n8n 是工作流编排/自动化平台，能搭 RAG 但需要你自己拼“知识库治理与产品体验”；Dify 把知识库当一等公民（Dataset/Chunk/检索策略/metadata filter/测试面板/KB API）。同时给出更现实的组合：Dify 做知识库与检索治理 + n8n 做抓取/清洗/切分/打标签/入库的自动化流水线。
#
#---
#
### 2) 基于我们的讨论给出的结论（可执行版）
#
#### 结论 A：你的目标最合适的系统形态
#
#你要的是“**可控写作（你提供论点与资料）+ 稳定风格复现**”，因此最佳形态不是单一 prompt，而是：
#
#* **Skill（写作器）**：严格按“三段节奏 + 段内自足 + 机制链 + 收束余音”生成
#* **Skill（审稿器）**：按 rubric 打分，不足自动重写
#* **内容约束**：`materials_only`，没有材料就用不确定性语言，不编数据
#* **防复刻闸门**：避免“像到抄”，保障长期可用与风险可控
#
#这四件事合在一起，才能做到“长期稳定像”。
#
#---
#
#### 结论 B：要不要上知识库？上，但要上对
#
#* **短期试跑**：先用你现有的 skill + few-shot + 审稿器，就能达到“明显像且可控”。
#* **想最大程度稳定像**：再上 **双知识库**：
#
#  1. **风格库（style KB）**：作者文本切分成“动作片段”并打标签（hook/define/mechanism/counter/analogy/closure…），检索只做“取型”；
#  2. **内容库（content KB）**：你给的事实/案例/引用，只允许从这里取证据。
#     这会显著提升一致性、可维护性，并降低跑偏与编造。
#
#---
#
#### 结论 C：平台选择建议（按你现在的需求）
#
#* **如果你要 WebApp + API + 未来服务化**：优先 **Dify** 来承载双库与应用（写作器/审稿器/过滤策略）。
#* **如果你要自动化灌数据/定时更新/清洗切分**：用 **n8n 做入库流水线** 很合适。
#* **最实用的组合**：**Dify（KB 与应用） + n8n（抓取清洗入库自动化）**。
#  这不是“谁更好”，而是分工最贴合你的目标。
#
#---
#
#如果你下一步要“真正开始搭”，我建议按这个顺序推进（不需要你额外回答问题也能干）：
#
#1. 先用你给的 skill 模板跑 3 篇（每篇你提供 thesis + 3-5 facts + 1-2 cases），把 rubric 跑通；
#2. 再把作者语料切成动作片段，打 metadata，上 style KB；
#3. 最后再接 n8n 做自动化入库与版本管理。
```
