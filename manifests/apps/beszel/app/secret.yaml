apiVersion: v1
kind: Secret
metadata:
  name: beszel-agent-secrets
stringData:
  # 重要：KEY 是 SSH 公钥，需与 Hub 端匹配
  KEY: "${SECRET_BESZEL_KEY}"
  # 重要：TOKEN 为代理认证令牌，建议定期轮换
  TOKEN: "${SECRET_BESZEL_TOKEN}"
  # Hub 服务地址（含协议）
  HUB_URL: "${SECRET_BESZEL_HUB_URL}"
