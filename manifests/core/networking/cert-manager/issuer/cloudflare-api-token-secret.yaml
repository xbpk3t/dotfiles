apiVersion: v1
kind: Secret
metadata:
  name: cloudflare-api-token-secret
  namespace: networking
type: Opaque
stringData:
  # What：Cloudflare DNS API Token。
  # Why：用于 ACME DNS-01 校验；Token 需包含目标 Zone 的 DNS 编辑权限。
  api-token: "${CLOUDFLARE_TOKEN_DNS}"
