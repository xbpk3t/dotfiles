apiVersion: v1
kind: Secret
metadata:
  name: n8n-secrets
stringData:
  # 重要：数据库密码需与数据库实例保持一致
  DB_PASSWORD: "${SECRET_N8N_DB_PASSWORD}"
  # 重要：加密密钥需长期稳定且足够复杂，否则历史数据无法解密
  N8N_ENCRYPTION_KEY: "${SECRET_N8N_ENCRYPTION_KEY}"
