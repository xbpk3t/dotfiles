---
version: '3'

tasks:
  default:
    desc: "执行清理：store GC + 裁剪 7d 前的 generations"
    summary: "task nix:gc"
    cmds:
      - task: _size
      # 这个sudo 很重要，否则无法执行
      - sudo nix-collect-garbage --delete-older-than 7d
      - task: _size

  _size:
    cmd: echo "[usage] $(du -sh /nix/store)"
