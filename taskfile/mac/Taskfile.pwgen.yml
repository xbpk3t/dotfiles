---
version: 3

tasks:
  default:
    desc: "为指定网站生成密码"
    vars:
      WEBSITE: "{{.CLI_ARGS}}"
    cmds:
      - "pwgen {{.WEBSITE}}"
    preconditions:
      - sh: command -v pwgen
        msg: pwgen not found ()
