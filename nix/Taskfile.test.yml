version: '3'

tasks:
  default:
    desc: "Build and switch to current system configuration"
    cmds:
      - echo "Building and switching configuration..."
      - sudo darwin-rebuild switch --flake .

  info:
    desc: "Show system information"
    cmds:
      - echo "System information"
      - nix --version
