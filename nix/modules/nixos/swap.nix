# NixOS SWAP configuration
# Based on Linux-Optimizer project configurations for Ubuntu/Debian/CentOS/Fedora
# Contains SWAP configuration that can be shared between multiple hosts
{ ... }:

{
  # Shared swap configuration patterns
  # Note: Specific swap devices should be configured per-host
  # swapDevices = [ ];
}
