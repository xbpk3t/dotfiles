# DTM is kept as an optional Go microservice dependency.
# The default transaction store remains DTM's built-in boltdb; etcd is used for service discovery.

ms:
  Driver: dtm-driver-gozero
  Target: etcd://etcd:2379/dtmservice
  EndPoint: dtm:36790

LogLevel: info
